# API (Python) — пакет example_pkg

Документация автогенерируется из docstrings пакета `example_pkg` в `src/`.

## Модуль: example_pkg

::: example_pkg
    options:
      show_root_heading: true
      members_order: source
